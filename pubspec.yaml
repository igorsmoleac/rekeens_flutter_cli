name: rekeens_flutter_cli
description: A CLI tool for generating Flutter projects with custom architecture, presets, and feature generators.
version: 0.12.0
repository: https://github.com/igorsmoleac/rekeens_flutter_cli

environment:
  sdk: ^3.13.0

dependencies:
  args: ^2.7.0
  mason_logger: ^0.3.5
  meta: ^1.16.0
  path: ^1.9.1
  yaml: ^3.1.3
  yaml_edit: 2.2.4

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2

executables:
  rekeens: rekeens_flutter_cli
